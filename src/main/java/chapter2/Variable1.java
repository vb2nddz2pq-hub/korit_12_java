package chapter2;

public class Variable1 {
    public static void main(String[] args) {
        int level;
        level = 10;
        System.out.println(level);
    }
}
              /*
              byte 1바이트
              shot 2바이트
              int 정수 숫자 나이 온도 층스 (몇 번세는 용도인지)
              long 8바이트 인트보다큰숫자
              i, j, k, = 반복문
              sum = 누적합 (값을계속 누적)
              num = 숫자 하나
              index =   위치
              result = 결과
              total = 전체
              count = 개수
              char 문자 1개 'A' 'B' '한'
              String 문자열 표현할떄 이름이나 주소
              double = 소숫점이 있는 숫자를나타낼때
              boolean = true 냐 false냐 를나타내는타입
              add = 밑에있는 계산식을 실행해서 결과값을 가져와라
              for / while을 선택하는가?
              for = 횟수를 기준으로 특정 반복횟수가 되었을 때 반복을 정지하고 싶다면
              for = 같은 일을 반복해서 실행하고 싶을 때 쓰는 문법 (반복횟수 가정해져있을때)
              while = {} 안에 조건식이 true일동안 반복실행 false가되면 반복종료 조건문 참 거짓
              var =  int boolean String  변수타입을 직접안쓰고 자바가 추측하게하는 문법
              if = 조건이 참(true)일때만 실행되는코드
              else = (그렇지않다면) if조건이 (false)일때 실행되는 코드
              switch = else if 가 길어질떄 값으로만 비교할때 경우의수가 명확한 값들
              yield =
              변수 = 변하는값
              상수 = 변하지 않는 값( 1년이 12개월이나 원주율이 3.14처럼변하지않는 상수는 대문자를주로사용함)
              실수 = 소숫점이있는숫자
              정수 = 소숫점이없는숫자
              산술연산자 *(곱) /(나누기) %(나머지)
              대입연산자 ==(같은가) != (다르냐) >(크다) <(작다)  = 오른쪽에있는걸왼쪽에대입
              논리연산자 && = AND (둘다참이어야함)  || = OR (하나만 참이어도 참) ! = NOT (반대)  ? = 초건이 참이면 값1 아니면 2
              증감연산자 i++ (i상자 안에숫자 1더하기) i-- (i안에숫자 1빼기)
               git add .
                    git commit -m "feat: 20260107 inheritance / abstraction / interface"
                    git push
                    자격 증명 삭제
               */


